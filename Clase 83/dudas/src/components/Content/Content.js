import React from 'react';
// objetoConDatos = {contentText:"lalala"}
// Content(objetoConDatos);
const Content = ({contentText}) => {
    return(
        <p>{contentText}</p>
    );
};

export default Content;
